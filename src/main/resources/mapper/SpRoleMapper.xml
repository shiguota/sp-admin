<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evo.sp.basis.mapper.SpRoleMapper">
    <!--根据用户名获取当前用户拥有的角色-->
    <select id="getRoleByUname" parameterType="java.lang.String" resultType="com.evo.sp.basis.entity.SpRole">
        select r.role_name from sp_user u
        JOIN sp_user_role ur on u.id = ur.user_id
        JOIN sp_role r on ur.role_id = r.id
        WHERE u.name = #{name} GROUP BY r.role_name
    </select>
</mapper>
